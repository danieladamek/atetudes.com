baseURL: "https://atetudes.com/"
locale: "en-us"
title: "At-Etudes"
enableRobotsTXT: true

module:
  imports:
    - path: "github.com/imfing/hextra"

markup:
  goldmark:
    renderer:
      unsafe: true
  highlight:
    noClasses: false

menu:
  main:
    - name: "At-Etudes"
      pageRef: "/studies"
      weight: 1
    - name: "Blog"
      pageRef: "/blog"
      weight: 2
    - name: "Search"
      weight: 3
      params:
        type: "search"

cascade:
  params:
    images: ["/assets/og.png"]

params:
  description: "Interactive Fretboard Logic — see the shapes, hear the relationships, understand the fretboard. Living, interactive études for guitar: chords, scales, modes and intervals across the neck."
  theme:
    default: "light"
    displayToggle: false
  navbar:
    displayTitle: false
    displayLogo: true
    logo:
      path: "assets/logo.svg"
      width: 118
  page:
    width: "normal"
  footer:
    enable: true
    displayCopyright: true
    displayPoweredBy: false
  editURL:
    enable: false
