# Obsidian / Cowork program-management layer — never commit
notes/
🚀 *.md

# Built publications — rebuildable from generators/ by definition.
# The published interactive editions live (tracked) in static/studies/.
publications/

# Hub door builds — regenerable from hub/ by definition, same rule as publications/
hub/build/

# Hugo
public/
resources/
.hugo_build.lock

# Tooling / OS
.claude/
.DS_Store
.bevel/
.vscode/
scratch pad/

# Python
__pycache__/
*.pyc
.venv/
venv/
